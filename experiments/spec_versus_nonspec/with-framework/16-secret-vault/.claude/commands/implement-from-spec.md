# implement-from-spec

Read `spec.yaml` and `CLAUDE.md` in this directory, then implement `vault.py` following actions A1→A12 in order. After writing the file, run through the acceptance checklist manually. Do not add dependencies beyond `cryptography`.
